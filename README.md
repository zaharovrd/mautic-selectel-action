# MautiBox Selectel Deploy Action

GitHub Action для автоматического развертывания MautiBox (форк Mautic) на VDS в Selectel без необходимости ручной настройки.

## ✨ Особенности

- 🇷🇺 **Русский язык по умолчанию** - Установка MautiBox с русским языком "из коробки"
- 🚀 **Развертывание в один клик** - Установите MautiBox за минуты, а не часы
- 🖥️ **Автоматическое создание VDS** - Создает и настраивает серверы в Selectel
- 🔒 **Поддержка SSL/HTTPS** - Автоматические SSL-сертификаты от Let's Encrypt (при указании домена)
- 🐳 **На базе Docker** - Надежное, контейнеризированное развертывание с Apache
- 📧 **Готовность к email-рассылкам** - Преднастроен для email-маркетинговых кампаний
- 🎨 **Пользовательские темы/плагины** - Поддержка расширений через Composer или прямую установку с GitHub/ZIP
- 🏗️ **Пользовательские Docker-образы** - Автоматически создает образы с предустановленными плагинами (подход на этапе сборки)
- ⚙️ **Cron-задания** - Автоматизированные фоновые задачи для оптимальной производительности. Cron вынесен в отдельные очереди для масштабирования.
- 📊 **Базовый мониторинг** - Мониторинг Selectel, логи контейнеров и артефакты развертывания

## 🚀 Быстрый старт

### 1. Предварительные требования

- Аккаунт Selectel (https://vds.selectel.ru/) с API-токеном (https://developers.vscale.io/documentation/api/v1/)
- SSH-ключ для доступа к серверу (загруженный в ваш аккаунт Selectel)
- Доменное имя (необязательно, можно использовать IP-адрес)

**Как создать токен:**
1. Перейти в панель Selectel: Настройки → Управление токенами (https://my.selectel.ru/profile/tokens)
2. Нажать "Создать токен"
3. Указать Описание токена (например, "GitHub MautiBox Deploy")
4. Галочку "Сделать токен READ ONLY" не ставим, оставляем отключенным. Нам нужна запись.
5. Скопируйте токен (если потеряет, его можно будет посмотреть на странице Управление токенами по кнопке "показать")

### 2. Настройка секретов

Добавьте эти секреты в ваш репозиторий GitHub (`Settings` → `Secrets and variables` → `Actions`):

```
SELECTEL_TOKEN=your_selectel_api_token
SSH_PRIVATE_KEY=your_ssh_private_key
MAUTIC_PASSWORD=your_admin_password
MYSQL_PASSWORD=your_mysql_password
MYSQL_ROOT_PASSWORD=your_mysql_root_password
```

#### Руководство по настройке SSH-ключа

**🔐 Рекомендуется: создайте выделенный SSH-ключ для автоматизации**

Для безопасности создайте новый SSH-ключ специально для этого GitHub Action:

```bash
# Сгенерируйте новый SSH-ключ (без кодовой фразы для автоматизации)
ssh-keygen -t ed25519 -f ~/.ssh/mautibox_deploy_key -N "" -C "mautibox-github-action"

# Добавьте публичный ключ в ваш аккаунт Selectel
cat ~/.ssh/mautibox_deploy_key.pub
# Скопируйте этот вывод и добавьте его в панели управления Selectel → Настройки → Безопасность → SSH-ключи
```

**SSH_PRIVATE_KEY**: Содержимое вашего **выделенного** приватного SSH-ключа.
- Используйте новый ключ: `cat ~/.ssh/mautibox_deploy_key`
- Скопируйте все содержимое файла, включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`
- ⚠️ **Ключ должен быть БЕЗ кодовой фразы** для автоматизации.

**Преимущества для безопасности:**
- 🔒 Отдельный ключ ограничивает ущерб в случае компрометации
- 🚫 Может быть легко отозван без влияния на другие сервисы
- 📝 Четкий аудиторский след для доступа автоматизации
- 🔄 Может быть заменен независимо от других ключей

Убедитесь, что ваш публичный SSH-ключ уже добавлен в ваш аккаунт Selectel перед запуском экшена.

### 3. Создание Workflow

Создайте файл `.github/workflows/deploy-mautibox.yml`:

```yaml
name: Deploy MautiBox

on:
  workflow_dispatch:
    inputs:
      vps_name:
        description: 'Уникальное имя сервера (например, client-name)'
        required: true
        default: 'mautibox-server'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy MautiBox to Selectel
        id: deploy
        uses: ./
        with:
          # --- Параметры для Selectel ---
          selectel-token: ${{ secrets.SELECTEL_TOKEN }}
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          vps-name: ${{ github.event.inputs.vps_name }}
```

### 4. Развертывание

1. Перейдите на вкладку `Actions` вашего репозитория.
2. Выберите workflow "Deploy or Update MautiBox to Selectel".
3. Нажмите "Run workflow".
4. Введите имя для вашего VDS и нажмите "Run workflow".

## 📋 Входные параметры

### Обязательные

| Параметр | Описание | Пример |
|-----------|-------------|---------|
| `vps-name` | Имя для VDS в Selectel | `mautibox-production` |
| `email` | Email-адрес администратора | `admin@example.com` |
| `mautic-password` | Пароль администратора MautiBox (используйте секреты!)| `${{ secrets.MAUTIC_PASSWORD }}` |
| `mysql-password` | Пароль пользователя MySQL | `${{ secrets.MYSQL_PASSWORD }}` |
| `mysql-root-password` | Пароль root для MySQL | `${{ secrets.MYSQL_ROOT_PASSWORD }}` |
| `selectel-token` | API-токен Selectel | `${{ secrets.SELECTEL_TOKEN }}` |
| `ssh-private-key` | Приватный SSH-ключ для доступа к серверу | `${{ secrets.SSH_PRIVATE_KEY }}` |

### Необязательные

| Параметр | Описание | По умолчанию | Пример |
|-----------|-------------|---------|---------|
| `vps-size` | Конфигурация VDS в Selectel | `s-2vcpu-2gb` | `s-4vcpu-8gb` |
| `vps-region` | Локация Selectel | `ru-1` | `ru-2`, `ru-3` |
| `domain` | Пользовательский домен | _(используется IP)_ | `mautibox.example.com` |
| `mautic-version` | Версия Docker-образа MautiBox | `5.0-apache-php8.1` | `4.4-apache` |
| `mautic-port` | Порт для приложения MautiBox | `8001` | `8080` |
| `themes` | Пользовательские темы (пакеты Packagist или URL-адреса ZIP-архивов с GitHub с опциональными параметрами, по одному на строку) | `""` |
| `plugins` | Пользовательские плагины (пакеты Packagist или URL-адреса ZIP-архивов с GitHub с опциональными параметрами, по одному на строку) | `""` |
| `mysql-database` | Имя базы данных MySQL | `mautic` | `mautibox_prod` |
| `mysql-user` | Имя пользователя MySQL | `mautic` | `mautibox_user` |

## 📤 Выходные данные

| Параметр | Описание |
|--------|-------------|
| `vps-ip` | IP-адрес созданного VDS |
| `mautibox-url` | Полный URL для доступа к MautiBox |
| `deployment-log` | Путь к файлу лога развертывания |

## 🔌 Расширенная настройка плагинов и тем

Этот экшен поддерживает гибкую установку пользовательских плагинов и тем как из публичных пакетов Packagist, так и из приватных репозиториев GitHub.

### Формат конфигурации

#### Простые URL (публичные репозитории)

```yaml
themes: |
  vendor/theme-name:^1.0
  another-vendor/custom-theme:dev-main
  https://github.com/user/theme-repo/archive/refs/heads/main.zip

plugins: |
  vendor/plugin-name:^2.0
  another-vendor/custom-plugin:^1.5
  https://github.com/user/plugin-repo/archive/refs/heads/main.zip
```

#### URL с параметрами (приватные репозитории и пользовательские директории)

Для приватных репозиториев или пользовательских имен директорий используйте URL-параметры:

```yaml
themes: |
  https://github.com/company/private-theme/archive/main.zip?directory=CompanyTheme&token=${{ secrets.COMPANY_GITHUB_TOKEN }}
  https://github.com/vendor/premium-theme/archive/v2.1.0.zip?directory=PremiumTheme&token=${{ secrets.VENDOR_GITHUB_TOKEN }}
  vendor/public-theme:^2.0

plugins: |
  https://github.com/company/private-plugin/archive/main.zip?directory=CompanyPlugin&token=${{ secrets.COMPANY_GITHUB_TOKEN }}
  https://github.com/vendor/custom-integration/archive/v1.5.2.zip?directory=CustomIntegration&token=${{ secrets.VENDOR_GITHUB_TOKEN }}
  vendor/public-plugin:^1.0
  https://github.com/public-repo/mautibox-plugin/archive/refs/heads/main.zip?directory=PublicPlugin
```

### URL-параметры

| Параметр | Описание | Обязательно | Пример |
|-----------|-------------|----------|---------|
| `directory` | Пользовательское имя директории, куда будет извлечен пакет | Нет | `CompanyPlugin`, `CustomTheme` |
| `token` | Персональный токен доступа GitHub для приватных репозиториев | Для приватных репо | `${{ secrets.COMPANY_GITHUB_TOKEN }}` |

### Настройка токена GitHub

Для приватных репозиториев вам потребуется создать персональные токены доступа GitHub (Personal Access Tokens):

1. Перейдите в [Настройки GitHub > Персональные токены доступа](https://github.com/settings/tokens)
2. Нажмите "Generate new token" → "Fine-grained personal access token"
3. Выберите конкретные репозитории, к которым нужен доступ
4. Предоставьте право на чтение "Contents" (Содержимое)
5. Скопируйте токен и добавьте его в секреты вашего репозитория GitHub

**Важно**: Имена секретов не могут начинаться с `GITHUB_`. Используйте имена вроде:
- `COMPANY_GITHUB_TOKEN` (для репозиториев company/*)
- `VENDOR_GITHUB_TOKEN` (для репозиториев vendor/*)

### Полный пример Workflow

```yaml
- uses: mautibox-ru/deploy-action@v1
  with:
    vps-name: 'mautibox-with-plugins'
    email: 'admin@example.com'
    plugins: |
      https://github.com/chimpino/stripe-plugin/archive/refs/heads/6.x.zip?directory=StripeBundle&token=${{ secrets.CHIMPINO_GITHUB_TOKEN }}
      https://github.com/company/analytics-plugin/archive/v1.0.zip?directory=AnalyticsPlugin&token=${{ secrets.COMPANY_GITHUB_TOKEN }}
      vendor/public-plugin:^2.0
    themes: |
      https://github.com/company/premium-theme/archive/main.zip?directory=PremiumTheme&token=${{ secrets.COMPANY_GITHUB_TOKEN }}
      vendor/free-theme:^1.5
    # ... другие обязательные параметры
```

## 📁 Примеры

### Базовое развертывание
```yaml
- uses: mautibox-ru/deploy-action@v1
  with:
    vps-name: 'my-mautibox'
    email: 'admin@example.com'
    # ... другие обязательные параметры
```

### Продвинутый вариант с доменом и SSL
```yaml
- uses: mautibox-ru/deploy-action@v1
  with:
    vps-name: 'mautibox-production'
    vps-size: 's-4vcpu-8gb'
    domain: 'marketing.example.com'
    email: 'admin@example.com'
    # ... другие параметры
```

### С пользовательскими плагинами и темами
```yaml
- uses: mautibox-ru/deploy-action@v1
  with:
    vps-name: 'mautibox-with-plugins'
    email: 'admin@example.com'
    plugins: |
      https://github.com/chimpino/stripe-plugin/archive/refs/heads/6.x.zip?directory=StripeBundle&token=${{ secrets.CHIMPINO_GITHUB_TOKEN }}
      vendor/public-plugin:^2.0
    themes: |
      https://github.com/company/custom-theme/archive/main.zip?directory=CustomTheme&token=${{ secrets.COMPANY_GITHUB_TOKEN }}
    # ... другие параметры
```

## 🛠️ Устранение неисправностей

### Распространенные проблемы

**1. Ошибка прав API Selectel**
```
Error: You are missing the required permission ssh_key:read
```
- У вашего API-токена Selectel недостаточно прав.
- Проверьте, что галочка READ ONLY не стоит.
- Обновите ваш секрет `SELECTEL_TOKEN` новым токеном.

**2. Сбой SSH-подключения**
```
Error: Permission denied (publickey)
```
- **Самая частая причина**: Приватный SSH-ключ не соответствует ни одному ключу в вашем аккаунте Selectel.
  - Убедитесь, что ваш публичный SSH-ключ добавлен в Selectel: Настройки → Безопасность → SSH-ключи.
- Проверьте, что ваш приватный SSH-ключ правильно отформатирован в секретах (включая полные заголовки).
- Используйте SSH-ключ без кодовой фразы для автоматизации.
- Проверьте логи экшена для отладочной информации о проверке ключа.

**3. Домен не указывает на сервер**
```
Error: Domain example.com does not point to VPS IP
```
- Обновите А-запись вашего DNS, чтобы она указывала на IP-адрес VDS.
- Дождитесь распространения DNS (может занять до 24 часов).

### Руководство по устранению проблем с SSH-ключом

Если вы получаете ошибки `Permission denied (publickey)`, выполните следующие шаги:

**1. Проверьте ваш SSH-ключ**
```bash
# Получите отпечаток (fingerprint) из вашего приватного ключа
ssh-keygen -l -f ~/.ssh/mautibox_deploy_key

# Посмотрите ваш публичный ключ (чтобы добавить в Selectel, если его там нет)
cat ~/.ssh/mautibox_deploy_key.pub
```

**2. Проверьте SSH-ключи в Selectel**
- Перейдите в панель управления Selectel.
- Откройте раздел "Настройки" → "Безопасность" → "SSH-ключи".
- Сверьте отпечаток ключа, который вы получили на шаге 1, с отпечатками ключей в панели. Они должны совпадать.

**3. Проверьте формат ключа в секретах GitHub**
- `SSH_PRIVATE_KEY`: Должен включать заголовки и быть полным приватным ключом:
  ```
  -----BEGIN OPENSSH PRIVATE KEY-----
  b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAlwAAAAdzc2gtcn
  ... (остальное содержимое ключа) ...
  -----END OPENSSH PRIVATE KEY-----
  ```

**4. Протестируйте SSH-подключение вручную**
```bash
ssh -i ~/.ssh/mautibox_deploy_key root@YOUR_VPS_IP
```

### Как получить помощь

1. Проверьте логи развертывания, загружаемый после каждого запуска.
2. Подключитесь к вашему серверу по SSH: `ssh root@YOUR_VPS_IP`.
3. Посмотрите логи Docker: `docker-compose logs -f`.
4. Проверьте логи MautiBox: `docker-compose exec mautic tail -f /var/www/html/var/logs/mautic_prod.log`.

## 🔒 Безопасность

- Используется приватная сеть Selectel для компонентов.
- Автоматическое SSL/TLS-шифрование с помощью Let's Encrypt.
- Пароли базы данных управляются безопасно через секреты.
- Регулярные обновления безопасности через официальные Docker-образы.
- Только аутентификация по SSH-ключам.

## 📊 Мониторинг

Развертывание включает базовый мониторинг:

- Мониторинг VDS в Selectel (CPU, RAM, использование диска).
- Мониторинг состояния Docker-контейнеров.
- Логи приложения MautiBox.
- Артефакты с логами развертывания, загружаемые в GitHub Actions.
- Автоматизированные Cron-задания MautiBox для обслуживания.

## 🔄 Обслуживание

### Обновление MautiBox

Чтобы обновить MautiBox до новой версии:

1. Измените параметр `mautic-version` в вашем workflow.
2. Запустите workflow заново.
3. Экшен загрузит новый образ и перезапустит сервисы.

### Стратегия резервного копирования

Важные директории для резервного копирования:
- `/var/www/mautibox_data` - Файлы и загрузки MautiBox.
- `/var/lib/docker/volumes/mautibox_db-data` - Файлы базы данных.

## 🤝 Участие в проекте

Мы приветствуем ваш вклад! Пожалуйста, не стесняйтесь отправлять Pull Request.

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT - см. файл [LICENSE](LICENSE) для подробностей.

## 🙏 Благодарности

- [MautiBox.ru](https://mautibox.ru) - Open-source автоматизация маркетинга.
- [Selectel](https://selectel.ru) - Облачная инфраструктура.
- [Docker](https://docker.com) - Платформа для контейнеризации.
